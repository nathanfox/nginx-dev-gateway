# ============================================
# NGINX Dev Gateway Route Configuration
# Generated: Wed Sep 24 11:33:54 AM EDT 2025
# Developer namespace: nathan
# Stable namespace: default
# ============================================

# ============================================
# Services being debugged (in nathan)
# ============================================

# test-payment - YOUR DEBUG VERSION
location /api/test-payment/ {
    set $test_payment_upstream test-payment.${CURRENT_NAMESPACE}.svc.cluster.local:8080;
    proxy_pass http://$test_payment_upstream/;
    include /etc/nginx/includes/proxy.conf;
}

# ============================================
# Stable services (in default)
# ============================================

# kubernetes - STABLE VERSION
location /api/kubernetes/ {
    set $kubernetes_upstream kubernetes.default.svc.cluster.local:443;
    proxy_pass http://$kubernetes_upstream/;
    include /etc/nginx/includes/proxy.conf;
}

# test-users - STABLE VERSION
location /api/test-users/ {
    set $test_users_upstream test-users.default.svc.cluster.local:8080;
    proxy_pass http://$test_users_upstream/;
    include /etc/nginx/includes/proxy.conf;
}

# ============================================
# WebSocket routes (if needed)
# ============================================

# Uncomment and modify as needed:
# location /ws/notifications {
#     set $ws_upstream notification-service.${CURRENT_NAMESPACE}.svc.cluster.local:8080;
#     proxy_pass http://$ws_upstream/;
#     include /etc/nginx/includes/websocket.conf;
# }

