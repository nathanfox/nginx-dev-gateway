apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-gateway-routes
  labels:
    app: nginx-gateway
    component: config
data:
  # Example route configuration
  # Each .conf file defines a location block for NGINX

  example-routes.conf: |
    # ============================================
    # TYPICAL DEVELOPMENT SETUP:
    # - Debug 1-3 services in your namespace
    # - Use stable versions from default/staging
    # ============================================

    # # Example: Service you're debugging (in YOUR namespace)
    # # URL: http://localhost:8080/api/payment/...
    # location /api/payment/ {
    #     set $payment_upstream payment-service.${CURRENT_NAMESPACE}.svc.cluster.local:8080;
    #     proxy_pass http://$payment_upstream/;
    #     include /etc/nginx/includes/proxy.conf;
    # }

    # # Example: Stable services (in default namespace)
    # # URL: http://localhost:8080/api/users/...
    # location /api/users/ {
    #     set $users_upstream user-service.default.svc.cluster.local:8080;
    #     proxy_pass http://$users_upstream/;
    #     include /etc/nginx/includes/proxy.conf;
    # }

    # location /api/orders/ {
    #     set $orders_upstream order-service.default.svc.cluster.local:8080;
    #     proxy_pass http://$orders_upstream/;
    #     include /etc/nginx/includes/proxy.conf;
    # }

    # # Example: WebSocket route (in default namespace)
    # # URL: ws://localhost:8080/ws/notifications
    # location /ws/notifications {
    #     set $ws_upstream notification-service.default.svc.cluster.local:8080;
    #     proxy_pass http://$ws_upstream/;
    #
    #     # Required WebSocket configuration
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";
    #     proxy_buffering off;
    #     proxy_connect_timeout 7d;
    #     proxy_send_timeout 7d;
    #     proxy_read_timeout 7d;
    # }

    # Add your routes here - remember to use variables in proxy_pass!
    # See docs/routing-guide.md for detailed routing patterns