apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-gateway-routes
  labels:
    app: nginx-gateway
    component: config
data:
  # Example route configuration
  # Each .conf file defines a location block for NGINX

  example-routes.conf: |
    # Example route configurations - uncomment and modify as needed

    # # Example: Route to a service in the same namespace
    # # URL: http://localhost:8080/api/echo/...
    # # Routes to: echo-service.{namespace}.svc.cluster.local
    # location /api/echo/ {
    #     proxy_pass http://echo-service.${CURRENT_NAMESPACE}.svc.cluster.local:8080/;
    #     include /etc/nginx/includes/proxy.conf;
    # }

    # # Example: Route to a service in a different namespace
    # # URL: http://localhost:8080/api/users/...
    # # Routes to: user-service.dev.svc.cluster.local
    # location /api/users/ {
    #     proxy_pass http://user-service.dev.svc.cluster.local:8080/;
    #     include /etc/nginx/includes/proxy.conf;
    # }

    # # Example: WebSocket route
    # # URL: ws://localhost:8080/ws/notifications
    # # Routes to: notification-service.{namespace}.svc.cluster.local
    # location /ws/notifications {
    #     proxy_pass http://notification-service.${CURRENT_NAMESPACE}.svc.cluster.local:8080;
    #     include /etc/nginx/includes/websocket.conf;
    # }

    # # Example: Route with path stripping
    # # URL: http://localhost:8080/svc/billing/invoice
    # # Routes to: billing-service.dev.svc.cluster.local/invoice
    # location ~ ^/svc/billing/(.*)$ {
    #     proxy_pass http://billing-service.dev.svc.cluster.local:3000/$1$is_args$args;
    #     include /etc/nginx/includes/proxy.conf;
    # }

    # Add your custom routes here
    # Note: /health and /metrics endpoints are already defined in the main config