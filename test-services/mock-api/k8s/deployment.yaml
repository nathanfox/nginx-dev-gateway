apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-api
  labels:
    app: mock-api
    type: test-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mock-api
  template:
    metadata:
      labels:
        app: mock-api
        type: test-service
    spec:
      containers:
      - name: api
        image: mock-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: SERVICE_NAME
          value: "mock-api"
        - name: PORT
          value: "8080"
        resources:
          limits:
            memory: "128Mi"
            cpu: "100m"
          requests:
            memory: "64Mi"
            cpu: "50m"
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: mock-api
  labels:
    app: mock-api
    type: test-service
spec:
  selector:
    app: mock-api
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  type: ClusterIP